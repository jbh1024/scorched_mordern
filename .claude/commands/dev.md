개발 서버를 시작합니다.

1. `npm run dev`를 실행하여 클라이언트(포트 3000)와 서버(포트 2567)를 동시에 기동합니다.
2. 시작 후 클라이언트 접속 URL(http://localhost:3000)을 안내합니다.
3. 에러가 발생하면 원인을 분석하고 해결 방안을 제시합니다.

$ARGUMENTS가 있으면:
- "client" → `npm run dev:client`만 실행
- "server" → `npm run dev:server`만 실행
